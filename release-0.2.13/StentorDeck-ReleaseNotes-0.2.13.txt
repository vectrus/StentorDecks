StentorDeck 0.2.13
Installer: StentorDeck-Setup-0.2.13.exe
Date: 2026-07-19

Install over your current build. Library, settings, and MIDI map stay in %APPDATA%.

What's new
-----------
2026-07-20 - Ship 0.2.13: Mouse load/scrub/fader scroll (actually in the installer)
Fixes that only lived in source until now: library drag->waveform load (Electron MIME session), waveform scrub without broken pointer capture, channel/pitch fader + knob scroll via non-passive wheel. Install this build - restarting the old AppData install cannot pick up repo edits. R1.5, R4.1, R4.2, R2.2.

How to update
-------------
- Run StentorDeck-Setup-0.2.13.exe, or use Settings > Updates > Check for updates (needs latest.yml on GitHub).
- Open StentorDeck from the desktop shortcut (not only the source .bat).

Notes
-----
- Builds are unsigned - Windows SmartScreen may warn once.
- Auto-update requires a full GitHub Release with latest.yml (not Setup.exe alone).
