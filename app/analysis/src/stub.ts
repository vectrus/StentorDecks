/**
 * E5 will host OfflineAudioContext BPM/key/waveform/loudness here.
 * E1 only stubs the package so the workspace and build graph exist.
 */
export function analysisStubReady(): boolean {
  return true;
}

console.info('[analysis] stub package loaded (E5)');
