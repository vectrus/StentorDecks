# Intentionally minimal. Do NOT put runtime/target/disturl here — npm 11+
# treats them as unknown project config and will stop forwarding them.
# Use `npm run bootstrap` (sets Electron ABI for better-sqlite3) or Node 20/22
# with plain `npm install` + postinstall `rebuild:native`.
